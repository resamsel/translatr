## 1. Wire the Bun CLI into the local i18n workflow

- [x] 1.1 Update root `package.json`'s `i18n:pull`/`i18n:push` scripts to invoke the Bun CLI (`bun run cli/src/index.ts pull`/`push` — a relative path, not `--cwd cli`, which would break `.translatr.yml` resolution; see design.md) instead of `./src/python/translatr.py pull`/`push`, keeping the `&& cd ui && npm run i18n:pull`/`i18n:push` chaining unchanged
- [x] 1.2 Verify `npm run i18n:pull` and `npm run i18n:push` each complete successfully against a real (or local dev) Translatr instance, producing the same file layout the Python script used to — verified against a mock server using the repo's real `.translatr.yml` (endpoint/project/target `ui/apps/translatr/src/assets/i18n/{locale.name}.json`, no `.?` before the placeholder). This surfaced a real bug in `cli/src/commands/push.ts`'s `targetFilter`: its regex treated `(.\?)?{locale.name}` as "one optional char" instead of Python's actual "char-then-literal-`?`" group, so any `push.target` without a literal `?` before the placeholder (i.e. every real per-app config in this repo, not just the CLI's own generated default) silently matched zero files. Fixed the regex and added a regression test (`push.test.ts`); pull and push (with the fix) both re-verified working end-to-end against the real config, with all repo-tracked files restored via `git checkout` afterward

## 2. Publish CLI binaries from the release pipeline

- [x] 2.1 Add a `cli-binaries` job to `.github/workflows/release.yml` that runs after the existing `release` job, sets up Bun, runs `bun install && bun run compile:all` in `cli/`, and uploads each of the 5 `dist/bin/*` files via `actions/upload-release-asset@v1` using `release.outputs.upload_url`, with asset names `translatr-linux-x64`, `translatr-linux-arm64`, `translatr-macos-x64`, `translatr-macos-arm64`, `translatr-windows-x64.exe` — also added the `outputs: upload_url` mapping to the `release` job so the new job can reference `needs.release.outputs.upload_url`
- [x] 2.2 Verify the job's YAML is valid (`bun run compile:all` succeeds locally producing exactly those 5 filenames in `cli/dist/bin/`) so the upload step names line up — confirmed: `translatr-linux-x64`, `translatr-linux-arm64`, `translatr-macos-x64`, `translatr-macos-arm64`, `translatr-windows-x64.exe` all present, matching the workflow's asset names exactly

## 3. Rewrite the public installer

- [x] 3.1 Rewrite `install.sh` to detect OS (`uname -s`) and architecture (`uname -m`), map them to one of the published Linux/macOS asset names, download it from `https://github.com/resamsel/translatr/releases/download/${TRANSLATR_TAG}/<asset>` (or the `latest` release when `TRANSLATR_TAG` is unset), `chmod +x`, and move it to `${TRANSLATR_PREFIX}/bin/translatr` (keeping the existing sudo-fallback logic for non-writable install dirs) — remove the pip/easy_install/python-setuptools bootstrap entirely. Also fixed a latent bug in the writability check while rewriting it: the old script checked `-w "$(basename ${TRANSLATR})"` (a bare relative filename, essentially never meaningful); the rewrite checks `-w "$(dirname ${TRANSLATR})"` (the actual install directory), which is what the sudo-fallback logic was always meant to test. `TRANSLATR_TAG` default changed from `master` (a git branch, meaningless for release assets) to unset, meaning "latest release" via GitHub's `/releases/latest/download/<asset>` URL form; a specific tag uses `/releases/download/<tag>/<asset>`
- [x] 3.2 Make `install.sh` exit with a clear "unsupported platform" error (naming the detected OS/arch) when there is no matching published asset (e.g. 32-bit or an unlisted OS), per the spec's "Unsupported platform" scenario
- [ ] 3.3 Verify `install.sh` end-to-end against a real GitHub release's assets (or a manually created draft release for testing) on at least Linux and macOS, confirming `translatr -h` runs immediately after install with no other tools installed — **deferred**: creating a GitHub release (even a draft) is a public/shared-state action outside this session's scope, and no `v*` tag has been pushed yet for `cli-binaries` to have built real assets against. The install/download logic itself is covered by `install.test.ts` (stubbed `uname`/`curl`, both scenarios passing). Verify this for real the first time a tag is pushed after this change merges — same one-time bootstrapping gap already called out in design.md's Risks section.

## 4. Retire the Python CLI and its references

- [x] 4.1 Confirm no other file references `src/python/translatr.py` beyond `package.json`, `install.sh`, and `CHANGELOG.md` by running `grep -rn "translatr\.py\|python/translatr" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=cli` and handling any surprises found — confirmed clean: only `CHANGELOG.md` (historical, left untouched) and this change's own planning docs under `openspec/changes/sunset-python-cli/` reference it; `package.json` and `install.sh` no longer do (already updated)
- [x] 4.2 Update `CONTRIBUTING.md` and any other contributor docs that mention the Python translation script or its `pip install` prerequisites, pointing them at the Bun CLI (`cli/README.md`) instead — checked `CONTRIBUTING.md`, `README.md`, and `docs/`: none mention `translatr.py`, its pip prerequisites, or `i18n:pull`/`i18n:push` at all, so there is nothing to update
- [x] 4.3 Delete `src/python/translatr.py`, and verify via `git status`/`git log` that it's gone with no other file still requiring it (re-run the grep from 4.1 and confirm the file itself no longer appears) — `git rm` staged the deletion, `src/python/` no longer exists, and the re-run grep shows zero remaining references outside `CHANGELOG.md` (historical) and this change's own planning docs

## 5. Finalize the CLI capability

- [x] 5.1 Re-run the CLI's existing manual verification (typecheck, `bun run build`, `bun run compile:all`, and the push/pull-against-a-mock-server smoke test performed earlier) to confirm nothing regressed while other tasks were in progress, and record the result in the PR description — all green: `tsc --noEmit` clean, `bun run build` bundles successfully, `bun run compile:all` produces all 5 expected binaries, `bun test` in `cli/` passes 12/12 (including the new push-target regression test), `bun test install.test.ts` at repo root passes 2/2, and a manual pull+push against a mock server on the real `.translatr.yml` config both worked correctly (files restored via `git checkout` afterward, no repo content left modified)
