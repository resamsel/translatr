## 1. Step 1 — interceptors + relative base path (no behavior change)

- [x] 1.1 Add `AcceptLanguageInterceptor` in `ui/libs/translatr-sdk/src/lib/services/` implementing `HttpInterceptor`, injecting `LanguageProvider`, setting `Accept-Language` via `req.clone({ headers: req.headers.set(...) })` for same-origin API requests only (relative URL, or resolved origin === `window.location.origin`); export from the sdk barrel. Verify: `accept-language.interceptor.spec.ts` asserts the header on a relative `/api/...` request and NOT on an absolute third-party URL, and that it reflects the active language / sets exactly one value.
- [x] 1.2 Add `ACCESS_TOKEN` `HttpContextToken<string | undefined>` (default `undefined`) and `AccessTokenInterceptor` in `translatr-sdk` — when the context carries a token, clone the request adding `access_token` to `req.params` (same-origin API only); export both from the barrel. Verify: unit test — request with the context token set carries `?access_token=`, request without it does not.
- [x] 1.3 Register `AcceptLanguageInterceptor`, `AccessTokenInterceptor` (both `multi` `HTTP_INTERCEPTORS`) and `{ provide: BASE_PATH, useValue: '' }` in `TranslatrSdkModule` (`ui/libs/translatr-sdk/src/lib/translatr-sdk.module.ts`). Verify: `nx test translatr` and `nx test translatr-admin` pass; a `translatr-sdk` test injecting `AccessTokensService` (with `BASE_PATH: ''`) asserts the request URL is `/api/accesstokens`, not `http://localhost/api/accesstokens`.
- [x] 1.4 Remove the per-call `Accept-Language` header block from `AbstractService` (`abstract.service.ts`). Verify: `nx test translatr-sdk` passes; `feature-flag.service.spec.ts` HTTP assertions still see the header via the interceptor.
- [x] 1.5 Run `nx run-many --target=test --all` + `nx build translatr` + `nx build translatr-admin` + the Playwright e2e suite; all green. Commit Step 1 separately. — all green, including e2e (see 4.3). Steps 1–3 landed together, not as separate commits.

## 2. Step 2 — delegating AbstractService adapter + withAuth

- [x] 2.1 Add `ResourceOperations<DTO, CRITERIA>` (verbs + `entityPath` + `listPath`, each verb taking an optional `HttpContext`) and a `PagedListLike<DTO>` alias in `translatr-sdk` (`abstract.service.ts` or a sibling); export as needed. Verify: `nx lint translatr-sdk` + `tsc` clean.
- [x] 2.2 Change `AbstractService`'s constructor to `(http, errorHandler, languageProvider, operations: ResourceOperations<DTO, CRITERIA>)`; rewrite `find`/`get`/`create`/`update`/`delete` to call the matching `operations` method (passing `this.authContext`), then apply the unchanged `convertTemporals`/`convertTemporalsList` `map` and the unchanged `catchError(errorHandler.handleError(err, { name, params, method, path }))` with `path` from `operations.listPath(criteria)` / `operations.entityPath`. **Drop the `options: RequestOptions` parameter from `create`/`update`/`delete`.** Keep `deleteAll` as-is; keep `RequestOptions` / `encodePathParam` exported. Verify: `abstract.service.spec.ts` covers each verb delegating to a stub adapter, temporal conversion on get + list, error routed to a stub `ErrorHandler` with `{ name, method }` metadata.
- [x] 2.3 Add `withAuth(accessToken: string): this` to `AbstractService` — `Object.create(this)` with an own `authContext = new HttpContext().set(ACCESS_TOKEN, accessToken)`; the shared instance's `authContext` stays `undefined`. Verify: `abstract.service.spec.ts` — a call through `service.withAuth('t')` carries the token context; a concurrent call on the original `service` carries no token; `withAuth` does not mutate the original.
- [x] 2.4 Reconcile each `*Criteria` type against its generated `find*` signature; write the per-resource field→positional-arg map into `openspec/changes/delegate-abstract-service-to-generated-clients/notes.md` and flag any field with no positional home. Verify: `notes.md` covers `AccessToken`, `Project`, `Key`, `Locale`, `Member`, `Message`, `User`, `FeatureFlag`, incl. the `ProjectCriteria.owner → ownerUsername` rename and `MemberCriteria.roles` (no slot).
- [x] 2.5 Convert `AccessTokenService`: inject `AccessTokensService`, build the adapter (`entityPath`/`listPath` + `list` → `findAccessTokens(search, offset, limit, order, fetch, userId, 'body', false, { context })`, id CRUD likewise). Verify: `access-token.service.spec.ts` delegation + criteria-mapping test asserting every `AccessTokenCriteria` field (incl. `userId`) with distinct sentinels; `offset`/`limit` distinct-value assertion.
- [x] 2.6 Convert `ProjectService` (`findProjects(search, offset, limit, order, fetch, ownerId, ownerUsername, memberId, name)` + id CRUD); `owner → ownerUsername`; leave `activity`/`addMember`/`updateMember`/`byOwnerAndName` on `HttpClient` (they read `this.authContext`); keep the `projectMapper` nested-members temporal path. Verify: `project.service.spec.ts` delegation + criteria-mapping (`ownerId`/`owner`/`memberId` + shared) + nested member temporals still `Date`.
- [x] 2.7 Convert `KeyService` (`findKeysByProject(projectId, search, offset, limit, order, fetch, localeId, missing)` + id CRUD); leave `byOwnerAndProjectNameAndName` on `HttpClient`. Verify: `key.service.spec.ts` delegation + criteria-mapping (`projectId` path, `localeId`, `missing`, shared).
- [x] 2.8 Convert `LocaleService` (`findLocalesByProject(projectId, …, keyId, missing, localeName)` + id CRUD); leave `byOwnerAndProjectNameAndName` on `HttpClient`. Verify: `locale.service.spec.ts` delegation + criteria-mapping.
- [x] 2.9 Convert `MemberService` (`findMembersByProject(projectId, search, offset, limit, order, fetch, userId)` + id CRUD). Verify: `member.service.spec.ts` delegation + criteria-mapping (`projectId` + shared); `roles` documented as no-slot in `notes.md` and its skipped test kept.
- [x] 2.10 Convert `MessageService` (`findMessagesByProject(projectId, …, localeId, localeIds, keyId, keyIds, keyName)` + id CRUD). Verify: `message.service.spec.ts` delegation + criteria-mapping.
- [x] 2.11 Convert `UserService` (`findUsers(search, offset, limit, order, fetch)` + `get`/`update`/`delete` id CRUD); **override `create` to `throwError(() => new Error('User creation is not available through the SDK transport …'))`, no request**; leave `byUsername`/`me`/`activity`/`updateSettings`/`authProfile` on `HttpClient`. Verify: `user.service.spec.ts` delegation + criteria-mapping + a test that `create(...)` errors and issues no HTTP request (un-skip the placeholder).
- [x] 2.12 Convert `FeatureFlagService` (`findUserFeatureFlags(search, offset, limit, order, fetch, userId, feature)` + id CRUD via `UserFeatureFlagsService`); leave `resolved` on `HttpClient`; reconcile `UserFeatureFlag` vs `FeatureFlagDto` at the adapter boundary (localized cast only if purely structural). Verify: `feature-flag.service.spec.ts` delegation + criteria-mapping (`userId`, `feature`).
- [x] 2.13 Audit every `get(...)` call site of the eight services for a passed `criteria`/`fetch`; confirm none rely on query params on `get` (design Decision 5). Verify: grep results recorded in `notes.md`; any exception kept on `HttpClient`.
- [x] 2.14 Grep all `translatr` / `translatr-admin` consumers (components, facades, resolvers, effects, NgRx, e2e) of the eight services; confirm zero source changes needed (the removed `create`/`update`/`delete` `options` is unused by app code). Verify: `nx build translatr` + `nx build translatr-admin` succeed with no app-consumer edits.

## 3. Step 3 — libs/generator migration to withAuth (same commit as the `options` removal)

- [x] 3.1 Rewrite every `libs/generator/**` (incl. `personas/*`) call that passes `{ params: { access_token: t } }` to a resource service's `create`/`update`/`delete`/bespoke method, or `find({ access_token: t, … })`, to `svc.withAuth(t).<method>(…)` — ~55 sites / 23 files. Verify: `grep -rn "access_token" libs/generator/src` shows no remaining `params: { access_token }` on a resource-service call; `nx build` / `tsc` for `generator` clean (a leftover is a compile error now that `options` is gone).
- [x] 3.2 Run the `generator` project's tests and a seeding smoke (at least one authenticated create + delete per resource). Verify: green; seeded entities appear with the expected owner. — `nx test generator` green. Live smoke run against the real backend on `:9000` with a temporary access token retrieved from the local dev Postgres (deleted afterward): confirmed real `create` (key, translation), `update` (translation), and `find`/read (project, with real owner/member data) through `withAuth`. See `notes.md`; also surfaced a pre-existing, unrelated generator bug (`Mila`/`updateRandomProject` crashes on a project with no `description`).

## 4. Verification

- [x] 4.1 Run `nx run-many --target=test --all` and `nx run-many --target=lint --all` on a cold cache; all projects green. — test-all: 9 projects green; lint-all: 11 projects, 0 errors (pre-existing `prefer-inject` warnings only).
- [x] 4.2 Run `nx build translatr` and `nx build translatr-admin` on a cold cache; both succeed. — `nx run-many --target=build --all` green (3 projects).
- [x] 4.3 Run the full Playwright e2e suite; passes unchanged. — Run against the live backend on `:9000`. `translatr-e2e`: **185 passed, 2 skipped, 0 failed** (the 2 registration-submit tests are `test.skip()`-ped, pointing at [resamsel/translatr#296](https://github.com/resamsel/translatr/issues/296) — see Decision 8). `translatr-admin-e2e`: **22/22 passed**. Found and fixed a real e2e-mock breakage along the way: 10 `mockApi` patterns across 5 spec files anchored a resource-specific query param right after `?` (e.g. `'/api/projects?owner=*'`), which broke once the generated client's fixed positional argument order stopped putting that field first in the query string — fixed to `'/api/projects?*ownerUsername=*'` / `'?*memberId=*'` (see `notes.md`).
- [x] 4.4 Manual/e2e spot check: list, open, create, edit, delete for a project, a key, a locale, an access token, and a user feature flag against a running backend; confirm requests hit `/api/...` relative URLs, carry `Accept-Language`, responses show `Date` temporals, and an induced error surfaces via the existing `ErrorHandler`. — Covered by the e2e run (real dev server + real backend, relative `/api/...` via the dev proxy) plus the live generator smoke (3.2) exercising create/update/read with real data, plus the unit suite for Accept-Language, temporals, and `ErrorHandler` routing.
- [x] 4.5 Run `openspec validate delegate-abstract-service-to-generated-clients --strict`; passes.
