include classpath("application.conf")

db.default {
	driver = "org.postgresql.Driver"
	url = "jdbc:postgresql://localhost/translatr?reWriteBatchedInserts=true"
	username = "translatr"
	password = "translatr"
	logSql = true
}

play.modules {
	disabled += play.api.cache.EhCacheModule
#	disabled += play.api.i18n.I18nModule
#	enabled += i18n.I18nModule
}

translatr {
	# Currently available: google, github, facebook, twitter
#	auth.providers = [google, github, facebook, twitter]
}

#play.cache.createBoundCaches = false
