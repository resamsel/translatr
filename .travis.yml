language: java
jdk:
    - oraclejdk8
script:
    - sbt clean coverage test && sbt coverageAggregate
after_success:
    - sbt coveralls
cache:
    directories:
        - $HOME/.ivy2/cache
        - $HOME/.sbt/boot/scala-$TRAVIS_SCALA_VERSION
